import TextRemover from "@/components/text-remover/TextRemover";

export const metadata = {
  title: "Text & Thumbnail Remover | ToolboxHub",
  description: "Remove text, watermarks, and subtitles from any image or video with AI.",
};

export default function TextRemoverPage() {
  return <TextRemover />;
}
